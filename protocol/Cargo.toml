[package]
name = "protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.4", features = ["rt", "net", "io-util", "sync", "time", "macros", "rt-multi-thread"] }
anyhow = "1.0.98"
byteorder = "1.5.0"
rand = "0.9.1"
