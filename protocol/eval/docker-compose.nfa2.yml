services:
  server:
    build:
      context: .
    environment:
      - RUST_LOG=trace
    volumes:
      - ../target/release/examples:/app
    stop_grace_period: 1s
    command: /app/eval_nfa2_latency_server
    networks:
      - development
    
  client:
    build:
      context: .
    environment:
      - RUST_LOG=trace
    volumes:
      - ../target/release/examples:/app
    stop_grace_period: 1s
    command: /app/eval_nfa2_latency_client
    networks:
      - development
    depends_on:
      - server

networks:
  development:
    name: development
